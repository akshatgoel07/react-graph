# Phase 1 runs on the standard library only — boots fast, fails nowhere.
# Dependencies are added in the phase that first uses them:
#   Phase 3: nats-py
#   Phase 4: qdrant-client, google-genai, tree-sitter + tree-sitter-language-pack
# (listed here as a roadmap; uncomment as each phase lands)
#
# nats-py==2.9.0
# qdrant-client==1.12.1
# google-genai==1.5.0
# tree-sitter==0.23.2
# tree-sitter-language-pack==0.4.0
